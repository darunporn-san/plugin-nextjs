import { FormInput } from "../plugin";
import type { FormInputProps } from "../plugin/types";


const InputText = (props: FormInputProps) => {
    return <FormInput {...props} />;
};

export default InputText; 